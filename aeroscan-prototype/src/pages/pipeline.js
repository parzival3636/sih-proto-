/* Stub — Pipeline (Dev 3) */
function el(html) { const t = document.createElement('template'); t.innerHTML = html.trim(); return t.content.firstChild; }
export function renderPipeline() { return el('<div class="page-placeholder">Pipeline — Dev 3</div>'); }
